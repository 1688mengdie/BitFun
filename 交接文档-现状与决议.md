# 交接文档：现状与决议（R7 续接完成后）

> 生成日期：2026-08-01
> 用途：向指挥官交接当前仓库状态、用户决议、遗留问题，供重新规划。
> 数据来源：本次会话实测（git 命令输出、GitHub API、进程/磁盘检查），全部可复验。

---

## 一、当前状态总览

### 1. Git 与云端状态（实测）

| 项 | 值 | 证据 |
|---|---|---|
| 本地 main | `07fbc5e96` chore: remove stray commit_classify.txt temp file | git log |
| origin/main | `07fbc5e96`（与本地零差异，49 提交全部推送） | git rev-list = 0 |
| upstream/main（GCWing/BitFun） | `e2e60c303` feat(extensions): add OpenCode workspace references | git fetch 实测 |
| origin/bitfun-upstream-sync | `6267e0b1d`（纯 bitfun 改造交付分支） | git log |
| origin/taiji-quant | 存在（taiji 定制备份分支，207 文件） | git branch -r |
| 工作区 | 干净（git status 0 条目） | git status |
| upstream/main 树 | 零 taiji（src/crates/taiji 0 条目，Cargo.toml 零 taiji 行） | git ls-tree 实测 |

### 2. R7 清理已执行内容（提交 b3bd69f06，536 文件）

- 删除 src/crates/taiji/（量化-自媒体基座 309 文件）+ src/crates/infra/（taiji 系孤儿 crate 151 文件）
- 删除 web-ui LVPA 品牌 UI（49 文件：LvpaModeSwitch、lvpaModeStore、scenes/lvpa、lvpa 主题、Lvpa 组件、infrastructure/lvpa）
- 删除 default.config.toml、3 个 taiji workflow（taiji-ci/security/bench）、Dockerfile.taiji-engine
- 根 Cargo.toml 删 21 个 taiji member + 14 个 taiji 依赖；Cargo.lock 自动清理（50 处 → 0）
- 引用清理：根 package.json（4 脚本）、tests/e2e/package.json（4 脚本）、e2e.yml、vite.config.ts（品牌文案）、deny.toml（3 处）、生成文件重新生成（8 主题零 lvpa）
- 组C 门控完整保留：`cfg(feature="taiji")` 92 处、`cfg(not(feature="taiji"))` 2 处、`cfg!` macro 2 处、Cargo.toml `taiji=[]` feature 定义

### 3. 提交分离（用户决议：云端 commit 分两类）

- **taiji 定制类**（留在 origin/main 历史 + origin/taiji-quant 备份）：`8581bebf4` taiji 打包提交、`328242613`/`4b2463b24`/`e87d5a221` feat(quant) 系列、`b3bd69f06` R7 清理
- **bitfun 原生改造类**：45 个纯 bitfun 提交（flowchat/market/dispatch/cli 等）+ `b1f4efa75`（session-tree/RBAC/coordination/Legion，25 文件 +750/-54）

### 4. 上游交付分支 origin/bitfun-upstream-sync（6267e0b1d）

- 基线：upstream/main `e2e60c303`（最新）
- 内容：193 文件，+13680/-6929，**零 taiji 文件**；仅保留组C 门控（cfg feature + feature 定义）
- 已剔除 33 项：fork 独有文档/数据 23 项（R-*.md、docs/audit、docs/plans、test_data、量价时空/、.devcontainer）+ 9 项本地落后版本（v9 vs 上游 v10、workspace-reference 被本地 merge 抹掉，推了会回退上游）+ 1 项临时文件
- 位置：仅 origin（用户 fork），**未推 upstream**

---

## 二、用户决议（本次会话明确）

1. **本地基线无所谓**：taiji 工作区本地文件是否保留 taiji 定制不再作为决策依据，以云端为准
2. **云端 commit 分离**：taiji 定制改动与 bitfun 原生改造两类明确分开（已执行，见上）
3. **bitfun 改造交付上游**：基于上游 main 最新基线构造纯 bitfun 分支（已执行，在 origin/bitfun-upstream-sync）
4. **GCWing/BitFun 永远不要碰**（铁则）：不对 upstream 做任何写入操作——不 push、不提 PR、不改任何内容。上游交付只能由用户自己操作（从其 fork 提 PR）
5. **编译纪律**（用户投诉后承诺）：不擅自跑重型编译（cargo check --workspace / cargo test）；需要时最小范围（-p 单 crate）+ 先报资源状态；编译输出落文件
6. **target 清理已执行**：debug 269GB 已删（释放 250.98GB，E 盘空闲 71→322GB）；release 13.19GB 保留——**BitFun 客户端本体运行在 target/release/bitfun-desktop.exe，release 不可删**

---

## 三、遗留问题（待指挥官规划）

### 1. taiji-strategy 依赖断链（高优先级）

taiji-strategy（E:/finance-trading/lvpa/software/taiji-strategy）4 个成员全部 path 依赖已删目录：
- `taiji-engine = { path = "../../../taiji/src/crates/taiji/taiji-engine" }`（dvmi/magnet/thrust/risk）
- `taiji-executor = { path = "../../../taiji/src/crates/taiji/taiji-executor" }`（risk）

**当前 taiji-strategy 编译必失败**（no matching package named 'taiji-engine'）。选项：
- A：从 git HEAD 恢复 src/crates/taiji/taiji-engine + taiji-executor 及依赖链（本地回退部分 R7）
- B：改 taiji-strategy 依赖指向备份（origin/taiji-quant checkout 目录）
- C：暂不处理

### 2. 客户端编译崩溃机制（已定位未根治）

- 机制：大规模删除/编译触发 BitFun 内置 flashgrep 服务批量 spawn `git cat-file --batch` 挂起堆积（曾实测 66 进程，父进程 flashgrep serve --stdio），客户端卡死/崩溃
- 已缓解：target debug 清理降低 IO 冲击；编译纪律约束
- 未根治：flashgrep 的 git 集成在仓库大变更场景的进程回收问题（BitFun 内部问题）

### 3. 文档残留（按"历史记录保留"原则未删）

- docs/audit/ISSUE-LOG.md、docs/plans/persistent-output/（14 文件）、根目录 R-2~R-6 报告、量价时空/pipeline-gate-design.md、test_data/、.devcontainer（LVPA 品牌）——均在 origin/main 树中保留

### 4. EphemeralSubagent 预留能力（W-E 会话登记，2026-08）

**现状（侦察结论，零代码改动）**：
- `coordinator::create_hidden_subagent_session` 的 `is_ephemeral` 参数传 `true` 时创建 `SessionKind::EphemeralSubagent`；唯一调用点 `create_hidden_subagent_session_with_workspace` 传 `false`——**当前零 true 调用点**，该能力处于预留状态
- 生命周期语义：`EphemeralSubagent` 既非 transient 也非持久化（`should_persist_session_kind` 中 `Subagent=true` 持久化、`EphemeralSubagent=false`），**需显式 discard/delete 清理**，不随父会话自动回收

**接入条件（不硬造场景，需要时再接线）**：
1. 后台隐藏子代理创建处（后台任务/Warden 隐藏会话场景）确需"一次性、不落盘、不占列表"的子代理时，向 `create_hidden_subagent_session` 传 `is_ephemeral=true`
2. 必须配套 discard 生命周期：在对应后台任务完成/取消点显式清理（delete/discard），否则产生无法被列表枚举也无法被持久化恢复的孤儿会话
3. 接线前确认列表枚举（`list_sessions_with_options` 非持久化分支 filter）与统计口径已覆盖 `EphemeralSubagent` 变体

---

## 四、环境事实（规划参考）

- taiji 工作区 target 组成：release 13.19GB（运行中，不可删）、doc/tmp 忽略
- E 盘空闲 322GB、C 盘 39GB、内存 31GB（22.5GB 空闲）
- 用户级/机器级环境变量无 RUST_MIN_STACK 残留；会话内 RUST_MIN_STACK=8MB（Rust 默认值）
- ~/.cargo/config.toml 仅注释（无全局污染）
- 上游 BitFun 树零 taiji（与本地 main 树差异 = 纯 bitfun 改造内容）
