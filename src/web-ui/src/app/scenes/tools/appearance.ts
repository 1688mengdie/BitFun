import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance/types';

export const toolsAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'tools',
  parts: [{ id: 'root' }],
};
