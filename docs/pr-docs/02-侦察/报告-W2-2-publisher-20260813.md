# 报告：W2-2 publisher —— 多平台发布能力接入

- **日期**：2026-08-13
- **执行**：姬码锋（W2 批收尾补录）
- **状态**：完成 ✅

## 一、任务背景

W2 批（content/publisher/gbrain/knowledge-graph）之 publisher：多平台内容发布能力接入，副作用工具（dry-run 红线同 W1-5 alert）。

## 二、改动清单

### taiji-lvpa 源区（commit 0df4c27，已 push origin/master）

- **新增 `rpc/publish.rs`**：`publish.preview` + `publish.submit`——四平台适配器 + PublishScheduler（已被幻梦池六步管线接线 workshop_dream.rs 真实现），RPC 面聚焦显式触发
- 注册：Cargo.toml（publisher 依赖已存在零新依赖）+ rpc/mod.rs + lib.rs（register_degraded 之后）

### 开发版（commit d814797eb 合流，已 push origin/main）

- **quant_tools.rs**：`content_publish` 工具（复用 `taiji_rpc_call` 9527 通道，零新依赖）
- **注册六处全落位**：mod.rs 导出 + materialization.rs 实例化 + restrictions.rs 白名单 + registry.rs 三 manifest + tool-provider-groups `PRODUCT_TOOL_PROVIDER_GROUP_PLAN`

## 三、验证

| 项 | 结果 |
|---|---|
| taiji-server 编译/测试 | ✅ 通过 |
| 开发版分 crate check/test（--jobs 4） | ✅ 通过 |
| 9527 真实连通性 | ✅ preview 真实渲染；submit 仅 dry-run（零真实发布） |
| 编码验证 | ✅ BOM=False + U+FFFD=0 |

## 四、副作用红线（必须）

- **publish.submit dry_run=true 默认**（同 W1-5 alert）
- **凭证不回传**（四平台凭证只在服务端，不进工具响应）
- 测试全走 dry-run/降级路径零真实发布；**真实投递需显式 dry_run=false + 用户确认**

## 五、验收断言对照

- [x] publish.preview/submit RPC 域（源区 0df4c27）
- [x] content_publish 工具（六处注册）
- [x] dry-run 默认 + 凭证不回传
- [x] 真实链路 preview（非 mock）
- [x] 零新依赖（同仓已有）

## 六、遗留

1. 真实投递通道端到端验证（需显式确认后另行执行）
2. 平台凭证管理界面（后续轮）
