# 报告 W1-4 strategen（2026-08-13）

## 一、任务目标
taiji-lvpa 源区新增 `strategen` RPC 域（`strategen.generate` + `strategen.validate`，
taiji-strategen StrategyGenPipeline 五阶段），开发版追加 `quant_strategen` 工具并注册
六处。产出 yaml 含 `version:"1.0"` 与 P0 呼应。默认 mock refiner 无 LLM key 可跑。

## 二、交付清单

### taiji-lvpa（E:\taiji-lvpa，master=b21dca7）
| 文件 | 改动 |
|---|---|
| `crates/taiji/taiji-server/src/rpc/strategen.rs`（新增） | strategen.generate（StrategyGenPipeline 五阶段，mock refiner 缺省，spawn_blocking 执行）+ strategen.validate（HypothesisValidator）+ yaml version:"1.0" 断言 + 5 单元测试 |
| `crates/taiji/taiji-server/tests/rpc_strategen_test.rs`（新增） | 6 集成测试（走真实 build_router + POST /api/rpc） |
| `crates/taiji/taiji-server/src/rpc/mod.rs` | `pub mod strategen;` |
| `crates/taiji/taiji-server/src/lib.rs` | `rpc::strategen::register_strategen`（register_degraded 之后，注册顺序铁律） |
| `crates/taiji/taiji-server/Cargo.toml` | `taiji-strategen = { path = "../taiji-strategen" }` + `serde_yaml = { workspace = true }` |
| `crates/taiji/taiji-strategen/src/pipeline.rs` | **能力层修复**：StrategyGenResult.yaml 存 Stage 3 编译产物（PipelineConfig YAML 含 version:"1.0"）而非 Hypothesis 自身序列化（原 bug：final yaml 缺 version 字段，与 P0 契约不符） |

### 开发版（E:\finance-trading\lvpa\software\taiji，main=b0dcb1ccf）
| 注册点 | 改动 |
|---|---|
| `quant_tools.rs` | 新增 `quant_strategen` 工具（复用 taiji_rpc_call 走 9527 JSON-RPC，零新依赖）+ 2 测试 |
| `implementations/mod.rs` | re-export `QuantStrategenTool` |
| `product_runtime/materialization.rs` | `"quant_strategen" => Some(Arc::new(QuantStrategenTool::new()))` |
| `restrictions.rs` | Commander 白名单 `allowed_tools.insert("quant_strategen")` |
| `registry.rs` | 三处 manifest 断言：builtin（L608 后）/ deferred（L820 后）/ readonly（L880 后） |
| `tool-provider-groups/src/lib.rs` | tool_feature_group 映射 + PRODUCT_TOOL_PROVIDER_GROUP_PLAN（core.session 组） |

## 三、验证证据（三证据）

### 证据 1：分 crate check/test（--jobs 4）
```
taiji-lvpa:
  cargo check -p taiji-server --tests --jobs 4        → 通过（0 error）
  cargo test -p taiji-strategen --jobs 4              → 32 passed
  cargo test -p taiji-server --test rpc_strategen_test → 6 passed
  cargo test -p taiji-server --lib rpc::strategen     → 5 passed

开发版（--features product-full，因 agentic 模块在 agent-runtime feature 下编译）:
  cargo check -p bitfun-core --features product-full --jobs 4 → 通过
  cargo test -p bitfun-core --features product-full --lib quant_tools    → 13 passed（含 strategen_validation_requires_action_and_hypothesis）
  cargo test -p bitfun-core --features product-full --lib agentic::tools::registry → 19 passed（三处 manifest 断言全绿）
  cargo test -p bitfun-core --features product-full --lib agentic::tools::restrictions → 19 passed
  cargo test -p bitfun-tool-packs --jobs 4           → 11 passed
```
说明：默认 `cargo test` 无法编译 agentic（agentic 模块 `#[cfg(feature="agent-runtime")]`），
必须 `--features product-full`；并行执行者（W1-2/W1-3/W1-5）编辑中的 session_control_tool/
execution_engine 等文件曾产生 6 个无关错误，与我改动无关，已确认我的 6 处文件零错误。

### 证据 2：9527 真实连通性（非 mock，真实 taiji-server 进程 + HTTP 调用）
```
strategen.validate:
  {"jsonrpc":"2.0","id":1,"method":"strategen.validate","params":{"hypothesis":{...MA cross...}}}
  → result: {"is_valid": true, "lookahead_free": true, "errors": [], "warnings": []}

strategen.generate（mock refiner 五阶段全链路）:
  → yaml_version_ok: True          （yaml 含 version: '1.0'，P0 呼应 ✓）
  → rounds: 6                       （五阶段 + mock 精化迭代 ✓）
  → yaml 头部: name / version: '1.0' / bar_gen / data_source 结构齐全 ✓
  → round_logs: 6 条 ✓
```

### 证据 3：编码 UTF-8
```
strategen.rs / rpc_strategen_test.rs / quant_tools.rs → 全部 UTF-8 无 BOM、无非法字节
（PowerShell UTF8Encoding(strict) 校验通过）
```

## 四、红线条目
- --jobs 4：全部 cargo 命令使用 ✓
- 分 crate：taiji-lvpa 按 crate check/test，开发版按 feature 组合 ✓
- 禁全量 fmt：未运行任何 cargo fmt ✓
- 不 kill：未 kill 任何进程（仅停止自己启动的验证用服务器）✓
- 禁 force push：正常 commit + push ✓
- 先读后改：先读 W1-1 样板（pattern.rs/quant_tools.rs/六处注册点）再写 ✓
- 编码 UTF-8：全部新增文件无 BOM ✓

## 五、能力层修复说明（重要）
`taiji-strategen/src/pipeline.rs` 原 `generate()` 在 `improved` 分支存
`serde_yaml::to_string(&serde_yaml::to_value(&current_hypothesis))`（Hypothesis 序列化）
作为 StrategyGenResult.yaml → final yaml 缺 version/bar_gen/data_source/nodes 管线结构，
与 P0 策略管线契约不符。已修复为存 Stage 3 编译产物 `yaml_str.clone()`（PipelineConfig
YAML，含 version:"1.0"）。taiji-strategen 32 测试全绿确认无回归。

## 六、commit 记录
- taiji-lvpa：`feat(W1-4): strategen RPC 域 — strategen.generate/validate（taiji-strategen 五阶段接线，yaml version 1.0）`
- 开发版：`feat(W1-4): strategen 工具族 — quant_strategen（9527 通道，注册六处）`
