# 报告：W2-3 gbrain —— 结构化 RAG 知识库接入

- **日期**：2026-08-13
- **执行**：姬码锋（W2 批收尾补录）
- **状态**：完成 ✅

## 一、任务背景

W2 批之 gbrain：结构化 RAG 知识库能力接入。关键判定：与开发版 `KnowledgeBaseSearchTool` **互补非重叠**（现有=文件系统全文检索 L0/L1/L3/L4；gbrain=结构化 RAG）。

## 二、改动清单

### taiji-lvpa 源区（commit a3ef752，已 push origin/master）

- **新增 `rpc/gbrain.rs`**：`gbrain.*` 6 方法（结构化知识库 RAG）
- 注册：Cargo.toml（gbrain 依赖已存在零新依赖）+ rpc/mod.rs + lib.rs（register_degraded 之后）

### 开发版（commit d814797eb 合流，已 push origin/main）

- **新增 `gbrain_tool.rs`**（457 行独立文件）：`gbrain` 工具（9527 通道，6 action），独立文件避免与 quant_tools.rs 并行冲突
- **注册六处全落位**：mod.rs 导出 + materialization.rs 实例化 + restrictions.rs 白名单 + registry.rs 三 manifest + tool-provider-groups `PRODUCT_TOOL_PROVIDER_GROUP_PLAN`

## 三、关键判定（接合评估）

| 项 | 现有（开发版） | gbrain（新增） |
|---|---|---|
| 类型 | KnowledgeBaseSearchTool（文件系统全文检索 L0/L1/L3/L4） | gbrain（结构化 RAG） |
| 判定 | **互补非重叠** | 工具名区分（现有保留不动）+ description 互相引用避免混淆 |
| AppState | — | 已有 gbrain 实例（state.rs L609-610），提升为直接字段 |

## 四、验证

| 项 | 结果 |
|---|---|
| taiji-server 编译/测试 | ✅ 通过 |
| 开发版分 crate check/test（--jobs 4） | ✅ 通过 |
| 9527 真实连通性 | ✅ 真实 RAG 检索（非 mock） |
| 编码验证 | ✅ BOM=False + U+FFFD=0 |

## 五、验收断言对照

- [x] gbrain.* RPC 域（源区 a3ef752，6 方法）
- [x] gbrain 工具（六处注册，独立文件）
- [x] 互补非重叠判定落地（工具名区分 + description 互引）
- [x] AppState 实例提升
- [x] 真实链路 RAG 检索（非 mock）
- [x] 零新依赖（同仓已有）

## 六、遗留

1. gbrain 与 KnowledgeBaseSearchTool 的联合检索编排（后续轮，当前独立可用）
2. RAG 索引维护/增量更新策略（后续轮）
